import styled from 'styled-components';
import { BasicForm } from '@/styles/Form.style';
import { Error } from '@/styles/Input.style';

const Form = styled(BasicForm)``;

export const S = {
  Form,
  Error,
};
