import styled from 'styled-components';
import { checkbox } from '@/styles/Input.style';
import { checkbox_label } from '@/styles/Input.style';

const Checkbox = styled(checkbox)``;
const CheckboxLabel = styled(checkbox_label)``;

export const S = { Checkbox, CheckboxLabel };
