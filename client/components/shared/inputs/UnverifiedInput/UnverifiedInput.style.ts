import styled from 'styled-components';
import { Input } from '@/styles/Input.style';

const UnverifiedInput = styled(Input)`
  margin: 0 0.625rem;
  max-width: 100%;
`;

export const S = {
  UnverifiedInput,
};
