import styled from 'styled-components';

import { form, form_success, from_error } from '@/styles/Form.style';

const Form = styled(form)``;

const Success = styled(form_success)``;

const Error = styled(from_error)``;

export const S = {
  Form,
  Error,
  Success,
};
