import styled from 'styled-components';

const Home = styled.div`
  margin: 0 auto;
  width: 100%;
  max-width: 100rem;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export const S = {
  Home,
};
